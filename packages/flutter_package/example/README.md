# Examples

This directory contains examples that showcase how to use the package.

Each example is a small Flutter package that depends on `flutter_package` via a relative path.
Run them with `flutter test`.

## basic/
A minimal demonstration of `ExampleWidget`.

## advanced_example/
A slightly more complete `MaterialApp` setup around `ExampleWidget`.
